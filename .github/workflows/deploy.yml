name: Deploy React App

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: docker/login-action@v2
        with:
          username: ${{ new_secret.DOCKER_USERNAME }}
          password: ${{ new_secret.DOCKER_PASSWORD }}

      - uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ new_secret.DOCKER_USERNAME }}/myapp:latest

      - uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ new_secret.SERVER_HOST }}
          username: ${{ new_secret.SERVER_USER }}
          key: ${{ new_secret.SSH_PRIVATE_KEY }}
          script: |
            docker pull ${{ new_secret.DOCKER_USERNAME }}/myapp:latest
            docker stop vite-app || true
            docker rm vite-app || true
            docker run -d --name vite-app -p 80:80 ${{ new_secret.DOCKER_USERNAME }}/myapp:latest